<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.eletroseg.iadecclouvor">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.EditaLetraActivity"></activity>
        <activity android:name=".activity.ExibirLetraActivity" />
        <activity android:name=".activity.CadastrarHinoActivity" />
        <activity android:name=".activity.FotoActivity" />
        <activity android:name=".activity.ListaAvisosActivity" />
        <activity android:name=".activity.ConfiguracaoActivity" />
        <activity android:name=".activity.ListaCifrasActivity" />
        <activity android:name=".activity.ListaLetrasActivity" />
        <activity android:name=".activity.CadastroLoginActivity" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>